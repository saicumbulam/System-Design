# Databases and Messaging Streams

* My sql writes per second: 230/second
* Cassandra = 1 million writes per second. Each client system generates about 17,500 write requests per second, and we can scale

